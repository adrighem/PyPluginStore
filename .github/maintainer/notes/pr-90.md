# PR:90 - chore(master): release 2.16.0

Status: open; mergeable but currently `UNSTABLE`.

Author:
- `github-actions[bot]` opened the Release Please PR.

Intent:
- Publish release `v2.16.0`.
- Includes:
  - `feat: native theme support, version 1`
  - `fix: accept API bridge error responses`
  - local registry documentation updates

Assessment:
- Diff is generated release metadata only:
  - `.github/scripts/generate_plugin.py` version bump
  - `.release-please-manifest.json` version bump
  - `CHANGELOG.md` entry
  - `plugin.py` version bump
- CodeQL passed.
- `Validate Plugins` and `Generate Plugin XML Header` on the PR show `action_required` with no jobs; this appears to be GitHub workflow approval behavior for the bot-created PR, not a test failure.
- The same `Validate Plugins` workflow passed on `master` after the latest fix.

Verification:
- `pytest -q`: 178 passed locally after validator hardening.
- `python .github/scripts/validate_plugins.py`: passed for 254 plugins locally after validator hardening.
- `git diff --check`: passed.

Recommended next step:
- Decide whether to include the local validator hardening before release.
- If yes, commit/push that change first and let Release Please refresh this PR.
- Approve/run the pending PR workflows.
- If they pass, merge `PR:90` as the release source.

Public action:
- None taken.
