# PR:138 - Release 2.24.0

Status: merged and released as v2.24.0.

Provenance:
- Same-repository Release Please branch with one verified GitHub Actions bot
  commit and focused release-note correction `6c8b358`.
- Final head `6c8b358` was directly based on `master` `00de1dc`.
- The diff is limited to version markers, the manifest, generated `plugin.py`,
  and `CHANGELOG.md`.
- No workflow, permission, dependency, lockfile, installer, binary, or
  third-party action change is present.

Assessment:
- The 2.24.0 version and generated files are correct.
- The generated notes initially omitted the release's main user-facing
  behavior because behavior-changing commits used `refactor:` prefixes.
- The final changelog and PR body now describe direct host-certified
  Git-to-latest-Release migration,
  immutable shared dependency generations with uv copy mode, recoverable code
  and dependency activation, backend-owned management actions, actionable
  lifecycle status, transient activity, and bounded restart recovery.
- Merge commit `6007e86` published v2.24.0 with all three branding assets and
  the corrected notes.

Verification:
- Full sanitized suite before the documentation-only correction: 1,497 tests
  passed; the focused generated/identity suite after correction passed 39 tests.
- Generated `plugin.py` parity, Python compilation, and `git diff --check`
  passed.
- All 256 registry repositories passed live validation.
- Complete diff and provenance review found no suspicious or unrelated change.
- Final PR and post-merge Generate Plugin XML Header, Release Please, Ubuntu and
  Windows validation, and CodeQL workflows passed.

Recommendation:
- No further pull request or release action is required.

Public action:
- Pushed release-note correction `6c8b358`, updated the PR body, merged
  `PR:138`, and published v2.24.0 with explicit user approval.
