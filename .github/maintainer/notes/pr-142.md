# PR:142 - Release v2.24.3

Status: merged as `6a926e6`; v2.24.3 published.

Provenance:
- Same-repository Release Please branch.
- One GitHub Actions bot commit, `3c3b03d`, directly based on `cbffb6a`.

Assessment:
- Mechanical patch release for the `ISSUE:122` restore-wording fix.
- Diff is limited to the release manifest, generator version, generated runtime
  version markers, and changelog.
- No workflow, permission, dependency, binary, install, network, or unrelated
  changes.
- The generated changelog says `closes ISSUE:122` although the source commit
  used `Refs #122`; the issue was already closed, so this had no state effect.

Verification:
- Exact head: 1,525 tests passed in a sanitized environment.
- Generated-runtime parity, Python compilation, and `git diff --check` passed.
- CodeQL passed on the pull request.
- Ubuntu, Windows, live registry validation, generated-runtime verification,
  Release Please, and CodeQL passed after merge.
- v2.24.3 is tagged at `6a926e6`, published as a normal release, and contains
  all three branding assets.

Public action:
- Merged after explicit approval:
  `https://github.com/adrighem/PyPluginStore/pull/142`.
- Release:
  `https://github.com/adrighem/PyPluginStore/releases/tag/v2.24.3`.
