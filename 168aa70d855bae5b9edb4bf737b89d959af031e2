Adds canonical whole-wrapper tree certification, source-path-relative install manifests, strict Domoticz identity and Python compilation checks, no-wrapper asset support, forge-neutral repository binding across GitLab/Codeberg/Forgejo/Gitea/generic hosts, staging revalidation, and scanner/runtime ZIP parity. Verification: 833 automated tests passed excluding intentionally deferred generated plugin parity; Python compilation and diff check passed. Manual verification waived by user.
