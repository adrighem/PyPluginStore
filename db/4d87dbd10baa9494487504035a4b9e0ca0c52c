Conductor task: Add generation-directory registry/index caching, durable highest-sequence watermark, bundled bootstrap/expiry rules, atomic pointer recovery, and crash-injection tests. Added exact-byte generations with hashes, fsync/rename boundaries, fail-closed recovery, and historical lineage validation. Verification: 39 metadata-store tests and 113 combined metadata/registry regressions passed.
