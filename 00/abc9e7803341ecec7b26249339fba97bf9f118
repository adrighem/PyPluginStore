Implements direct DNS-pinned runtime HTTPS streaming with exact-origin redirect controls, full DNS-answer validation, exclusive durable artifact writes, member-by-member ZIP extraction, portable path checks, resource limits, and archive descriptor pinning. Verification: 289 archive/runtime HTTP/release metadata tests passed; Python compilation and diff check passed. Manual verification waived by user.
